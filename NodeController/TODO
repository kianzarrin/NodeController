
- twisted segments need slope when attached at an angle. (fixed)

- options for default universal behaviours:
    - limit dir.y to 2 (done)
    - sloped junction (done)
    - auto embankment for curved segments. (not done)
    - if segment count=2, only one side should have zebra crossing by default (done in TMPE)

- open and close left and right table.
- new Corner table organisation

- test selecting nodes on top of another road (FRT + anarchy +  moveIT). (can't reproduce)

- corner offset to increase chances of train track connection.
- fix the orientation of asym road
- support for asymetric textures
- lod support (not now)

- Toggle Crossing tool mod activatble from UUI.

